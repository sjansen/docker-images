#!/bin/bash
set -e

NAMESPACE="sjansen"
IMAGE=$(git rev-parse --abbrev-ref HEAD | tr / :)
TAG="$NAMESPACE/$IMAGE"

mkdir -p /tmp/sbt
sudo chcon -Rt svirt_sandbox_file_t /tmp/sbt || true
sudo docker run -it --rm \
  -v /tmp/sbt:/app \
  "$TAG" sbt "$@"
